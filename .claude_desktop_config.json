{
  "mcpServers": {
    "chrome-devtools": {
      "command": "npx",
      "args": ["chrome-devtools-mcp@latest", "--channel", "stable", "--isolated"]
    },
    "playwright": {
      "command": "npx",
      "args": ["@playwright/mcp", "--browser", "chrome", "--viewport-size", "1920,1080"]
    }
  }
}